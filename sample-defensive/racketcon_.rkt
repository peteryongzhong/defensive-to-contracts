#lang racket

(define (url-secure? url)
  #t)

(define (url-local? url)
  #t)
(define (url? url)
  #t)
(define (url-string? url)
  #t)

(define (string->url url)
  #t)

(define (somecode)
  (void))

(define (someothercode )
  (void))

(provide (all-defined-out))